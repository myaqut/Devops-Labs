pipeline {
    agent any 
    stages {
        stage('build') {
            steps {
                sh 'echo "7amo" '
                sh 'cd flaskapp'
                sh 'python3 --version'
            }
        }
    }
}

